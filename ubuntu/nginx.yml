---
- hosts: all
  gather_facts: no
  sudo: yes
  vars:
  tasks:
  - name: install nginx
    apt: name=nginx state=present
  - name: start and enable nginx
    service: name=nginx enabled=yes state=started
