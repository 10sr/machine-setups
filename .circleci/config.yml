---
version: 2
workflows:
  version: 2
  test:
    jobs:
      - test-2.7
      - test-3.6

jobs:
  test-2.7:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run:
          name: Run tests
          command: cd sakura && make install check
  test-3.6:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          name: Run tests
          command: cd sakura && make install check
